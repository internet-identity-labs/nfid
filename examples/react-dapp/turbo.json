{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "pipeline": {
    "build": {
      "env": [
        "VITE_MOCKED_SIGNER_PROVIDER_URL",
        "VITE_TARGET_CANISTER"
      ]
    }
  }
}